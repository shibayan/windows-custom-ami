machine:
  environment:
    AWS_DEFAULT_REGION: ap-northeast-1

dependencies:
  pre:
    - sudo pip install awscli
  override:
    - aws ec2 run-instances --dry-run --cli-input-json file://run-instances.json --user-data file://user-data.txt