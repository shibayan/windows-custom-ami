machine:
  environment:
    AWS_DEFAULT_REGION: ap-northeast-1

dependencies:
  pre:
    - sudo pip install awscli
  override:
    - aws ec2 run-instances --cli-input-json file://custom-ami.json --user-data file://userdata.txt